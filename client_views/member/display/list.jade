table(cellpadding="0", cellspacing="0")
  tr
    th Full Name
    th Email

    if band_admin
      th

  each member in band_members
    tr.display(member_id="#{member.id}")
      td #{member.full_name}
      td #{member.email}

      if band_admin
        td.delete &#x2327;
