table(cellpadding="0", cellspacing="0")
  tr
    th Band Name
    th
  each band in person_bands
    tr.display(band_id="#{band.id}")
      td #{band.name}
      td.delete
        input(type="button", value="-", name="delete")
